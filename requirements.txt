# EigenTrace direct dependencies (floors only). Exact production versions: requirements.lock.txt.
# torch: production uses the CUDA 12.1 build. Either install from requirements.lock.txt or add
#   --extra-index-url https://download.pytorch.org/whl/cu121
chromadb>=0.5
diffusers>=0.31
httpx>=0.28
nltk>=3.9
numpy>=1.26,<2          # production is 1.26.4; numpy 2 is untested with this chromadb/torch pair
python-dotenv>=1.0
requests>=2.32
rich>=13
sentence-transformers>=5.0
torch>=2.5
trafilatura>=2.0
wordfreq>=3.1
spacy>=3.8              # plus the en_core_web_sm model (see requirements.lock.txt)
beautifulsoup4>=4.10
websocket-client>=1.8
piper-tts>=1.4          # CLI used by segment_player.py
