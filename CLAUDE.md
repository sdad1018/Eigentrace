# EigenTrace on Bertha

- **Two trees, never conflated.** The canonical code tree is `/mnt/c/Users/M4ISI/eigentrace`; the data tree is `/home/remvelchio/eigentrace`. Keep them distinct in every path, command, and reference.
- **Recon before build.** Read before proposing, and propose before patching.
- **Backup before any destructive change.** Use anchor-based patches only.
- **`stream/` and `dream_broadcast.py` touch the live broadcast.** No edits there without explicit approval given in the same turn.
- **Secrets live outside this tree.** Never read, print, or echo any variable containing `KEY`, `TOKEN`, or `SECRET`.
- **Label every claim on public artifacts** as either *measured* or *argued*.
